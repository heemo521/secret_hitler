import React from 'react';
import PropTypes from 'prop-types';

function Home(props) {
  // display name set here before entering to the lobby right away
  // in the home it should include game description and link to the lobby
  // in the lobby the top bar will have a place to enter room code or search by name
  // with two buttons on says join or just write in the search bar to find the room
  return <div>Home</div>;
}

Home.propTypes = {};

export default Home;
